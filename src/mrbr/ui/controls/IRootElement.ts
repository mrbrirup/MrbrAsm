export interface Mrbr_UI_Controls_IRootElement {
    /**
     * Control's root element. Only one root element per control. Interface used to ensure that all controls have a root element that can be mounted to the DOM.
     * @date 31/10/2022 - 14:14:42
     *
     * @type {HTMLElement}
     */
    rootElement: HTMLElement;
}